[project]
name = "vodle"
version = "0.1.0"
description = "A command-line daily word deduction game called Vodle."
authors = [{ name = "Vodle Team" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
vodle = "vodle.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
